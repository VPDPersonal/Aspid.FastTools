# SerializeReference — что добавить, чтобы стать пакетом №1

> Рабочий документ ветки `feature/serialize-reference-dropdown`. Создан 2026-07-01, обновлён 2026-07-02 (статусы плана + ревью P0).
> Компаньон к бывшему PLAN.md (UI/UX-задачи; файл удалён после закрытия всех 8 пунктов — снимок статусов ниже).

## Где мы уже лидируем

По **надёжности и обслуживанию** SerializeReference ветка уже, вероятно, лучшая на рынке — этого нет ни у одного бесплатного пакета (mackysoft `SubclassSelector`, TriInspector, Alchemy), а частью и у Odin:

- Ремонт missing-type прямо через YAML (SO, префаб-ассеты, Prefab Mode, чистая сцена).
- Проектный свип битых ссылок + группировка по типу + **Fix all**.
- Детект shared/aliased ссылок, rid-цвета, **Make Unique** / **Link to Existing**.
- Build/CI-gate + Required-gate + breakage-детектор + уведомления.
- Type Usage Index / Find Usages / delete-guard.
- Полный паритет UIToolkit ↔ IMGUI (с оговоркой по п.1 ниже).
- Дженерики (открытый `Modifier<T>` с выбором `T`), copy/paste, keep-data.
- `[TypeSelectorDisplay(Tooltip, Icon)]`, favorites, MRU, поиск, breadcrumbs.
- Create New Script из пикера, cross-domain-reload назначение.

Пробелы — не в «прочности», а в **authoring-UX-полировке** (сильны Odin/TriInspector), **автоматизации рефакторинга** (никто не закрыл толком) и **охвате/экосистеме**.

---

## Статус текущего плана (снимок 2026-07-02)

Все 8 пунктов PLAN.md закрыты:

| # | Пункт | Статус |
|---|---|---|
| 1 | «Make unique» → в самый низ | ✅ Готово — IMGUI синхронизирован с UIToolkit (shared под `DrawChildren`) |
| 2 | Auto-fix «Missing type» в примере | ✅ Готово (+ Smart Fix через `MovedWeaponPreset`, 2026-07-02) |
| 3 | Settings-таб — общий стиль | ✅ Готово — `EnumField` gate застилен в `Aspid-FastTools-Settings.uss` |
| 4 | TypeSelector — шапка не закруглена | ✅ Готово |
| 5 | Ctrl+Tab / Ctrl+Shift+Tab по вкладкам | ✅ Готово (`5a25eaa`, проверено вживую) |
| 6 | «Excluded folders» — список + селектор | ✅ Готово |
| 7 | TypeSelector — тусклое выделение строки | ✅ Готово |
| 8 | Вынести YAML-движок в отдельную ассембли | ✅ Готово (SR-scoped) |

---

## A. Хвосты из текущего плана

> **2026-07-02:** все S-хвосты (п.1, п.3, п.5) закрыты — см. снимок выше. Остался единственный L-пункт, осознанно вынесенный в отдельный будущий заход (не для этой ветки):

| Источник | Что доделать | Эффект | Объём |
|---|---|---|---|
| п.8 | Промоушен YAML в общий `…Editor.Yaml` + 2-й потребитель (`m_Script`-GUID repair) | Движок становится отдельной публичной фичей | L |

---

## B. Новые фичи для позиции №1

> Формулировки №1–3 уточнены по итогам ревью 2026-07-02 (проверка посылок по коду) — детали в «Заметках ревью» под таблицей.

| # | Приоритет | Фича | Что именно | Эффект | Готовность фундамента |
|---|---|---|---|---|---|
| 1 | ✅ Готово (2026-07-02) | **Batch-миграция переименований** *(бывш. «авто-миграция `[MovedFrom]`»)* | Реализовано: `SerializeReferenceMovedFromResolver` (авторитетный резолвер, отказ при неоднозначности/закрытых генериках), классификация в breakage-отчёте и тосте, info-карточка **Migrate all (N) → Type** в Project References (confirm + diff + undo, с гейтом по констрейнту поля), демо в сэмпле (`Crossbow` + `RenamedWeaponPreset.asset`) | Закрывает главную боль SR: «протухший» YAML на диске + возможность удалить `[MovedFrom]` после запекания | реализовано на ранкере + YAML-rewriter + свипе |
| 2 | ✅ Готово (2026-07-02) | **Имя + группировка в пикере** | Реализовано: `Name` и `Group` в `TypeSelectorDisplay` — кастомный лейбл (генерики сохраняют `<T>`, поиск матчит и реальное имя), явные `/`-пути вместо namespace, дизамбигуация коллизий именем типа | Закрыто отставание даже от **бесплатного** mackysoft `[AddTypeMenu]`; паритет с Odin/TriInspector | реализовано расширением готового атрибута |
| 3 | 🟡 P1 *(⬇ из P0, 2026-07-02)* | **Default-инстанс в точках создания** *(бывш. «non-null авто-инстанс»)* | `TypeSelector(DefaultType=typeof(Sword))` — default применяется **только в явных точках создания** (новый элемент списка, плашка «Initialize as …» на пустом поле), без тихого присвоения при отрисовке | Убирает класс NRE и пустых полей | ★★☆ рядом с `Required`, но края: `<None>`, списки, prefab-override |
| 4 | 🟡 P1 | **Свёрнутое inline-превью** | Одна строка-сводка инстанса при закрытом foldout | Фирменная читаемость Odin | ★★☆ |
| 5 | 🟡 P1 | **Семантический цвет по типу** | Цвет-акцент из атрибута (отдельно от rid-цвета shared) | Различать варианты визуально | ★★★ есть механика цвета/полосы |
| 6 | 🟡 P1 | **Богатая фильтрация** | exclude-list, include/exclude по assembly/namespace, скрытие `[Obsolete]`, кастомный предикат | Сейчас только сужение по базовому типу | ★★☆ есть `BuildAssignableFilter` |
| 7 | 🟡 P1 | **Вложенные Templates** | Сохранять вложенные `[SerializeReference]`-дети (сейчас только 1 уровень) | «Пресет сложного объекта целиком» | ★★☆ clipboard уже хранит полный JSON |
| 8 | 🟢 P2 | **Персистентный clipboard** | Copy/Paste переживает перезапуск редактора (сейчас только сессия) | Меньше потерь при рефакторинге | ★★★ |
| 9 | 🟢 P2 | **Reorder с бейджем типа** | В полиморфных списках показывать тип на элементе при drag | Списки читаются с первого взгляда | ★★☆ |
| 10 | 🟢 P2 | **Runtime-API резолвинга** | Публичный рантайм-хелпер «имя → `Type` → инстанс» (сейчас всё в editor) | Моддинг / data-driven | ★☆☆ новый рантайм-слой |
| 11 | 🟢 P2 | **Версионирование данных** | Колбэк-апгрейд при смене схемы типа (поле-версия + миграция) | Старые ассеты не «протухают» | ★☆☆ |
| 12 | 🔵 доки | **Маркетинг/дискаверабилити** | Таблица-сравнение vs Odin/TriInspector/mackysoft в README, видео, OpenUPM/Asset Store, CHANGELOG | «№1» = ещё и заметность; сильнейшая часть (ремонт/свип) недопродана | — |

### Заметки ревью P0 (2026-07-02)

**№1 — почему переформулирована:**

- Unity **нативно** мигрирует managed references по `[MovedFrom]` при загрузке (проверено вживую на Smart Fix-демо — см. PLAN.md п.2): ассет с рабочим атрибутом не ломается, так что старый триггер «100% по `[MovedFrom]`» почти никогда не выстрелил бы. Ветка `MatchesOldIdentity` резолвера (`SerializeReferenceMovedFromResolver`) (score 1.0) — страховка для краевых случаев, не основной сценарий.
- Нативная миграция чинит только **в памяти**: YAML на диске держит старое имя до пересохранения ассета → никогда не загружаемые ассеты, CI-сканы и диффы остаются «битыми». Отсюда режим «bake `[MovedFrom]` в диск» — после прогона атрибут можно удалить из кода.
- «Молча переписать» противоречит зафиксированному продуктовому решению (doc-коммент ранкера: *«The suggestion is never auto-applied — the user always clicks»*). Score 1.0 достижим и чистой эвристикой (0.8 «same type name» + 0.2 field-shape — кейс `MovedWeaponPreset`), а она не авторитетна → только batch с подтверждением.
- Часть фичи уже существует: проектный свип + группировка по типу + **Fix all** (`SerializeReferenceProjectView`), breakage-детектор как триггер. Реальная дельта — связка «детект → одна кнопка Migrate N → отчёт + undo» и bake-режим.

**№2 — нюансы дизайна (не блокеры):**

- Поиск должен матчить и display-имя, и CLR-имя типа.
- Дизамбигуация двух типов с одинаковым display-именем (механика уже есть — `AddTypesWithDisambiguation` в `HierarchyBuilder`).
- `Group` **заменяет** namespace-путь для типа в дереве, а не дублирует его.

**№3 — почему понижена в P1 и сужена:**

- «Поле никогда не `null`» — оверпромис: гарантия действует только на editor-путях через дровер; рантайм-создание объектов остаётся `null`.
- Острые края тихого авто-инстанса: dirty-ассет от простого открытия инспектора (VCS-шум); конфликт с явным выбором `<None>` в пикере; новые элементы списков — рядом со свежепочиненным UUM-129100; непрошеные overrides на инстансах префабов; пересечение с `Required` (DefaultType делает его бессмысленным на том же поле — нужна явная политика).
- Безопасная семантика: default применяется только в явных точках создания — без присвоений при отрисовке.

---

## Рекомендуемый порядок

1. ~~**Хвосты A** (S-объёмы, быстро закрывают долги плана).~~ ✅ Закрыты 2026-07-02 (кроме YAML-промоушена — отдельный заход).
2. ~~**B: P0** — сначала **№2** (Name + Group: дешевле всех, посылка точная, без дизайн-конфликтов), затем **№1** (batch-миграция — в переформулированном виде).~~ ✅ Оба закрыты 2026-07-02 (код + тесты + доки + сэмпл-демо); прогнать Unity-тесты в редакторе.
3. **B: P1** (3–7) — полировка «до уровня Odin»; №3 (DefaultType) понижена сюда по итогам ревью, делать с суженной семантикой.
4. **B: P2 + доки** — охват и экосистема.

## Точки врезки (для P0 №1 — batch-миграция переименований)

- Ранжировщик подсказок: `SerializeReferences/Extensions/SerializeReferenceRepairSuggestions.cs` — `[MovedFrom]` даёт 1.0; тот же 1.0 достижим как 0.8 «same type name» + 0.2 field-shape.
- Триггер: `SerializeReferences/Diagnostics/SerializeReferenceBreakageDetector.cs` (обнаружение newly-missing).
- Применение: YAML-rewriter в `SerializeReferences/Yaml/` (`SerializeReferenceYamlEditor.TryRewriteType`).
- Проектный охват: `SerializeReferences/Index/SerializeReferenceTypeUsageIndex.cs` + Project-свип с готовым **Fix all** (`SerializeReferences/Windows/SerializeReferenceProjectView.cs`).
